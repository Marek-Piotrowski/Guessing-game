@using Guessing_Game.Models
@model Person

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit Person</h1>

@if (User.Identity.IsAuthenticated)
{
    <form asp-action="Edit"  method="post">
    <div asp-validation-summary=All class="text-danger"></div>
    <div class="form-group">
        <label>Name</label>
        <input name="NewName" type="text" placeholder=@Model.Name />
    </div>
    @*<span asp-validation-for=@Model.person class="text-danger"> </span>*@
    <div class="form-group">
        <label>Phone Number</label>
        <input  name="NewPhone" type="text" placeholder=@Model.PhoneNumber  />
    </div>
    <div class="form-group">
        <input  name="PersonId" type="hidden" value=@Model.PersonId  />
    </div>

    @*<span asp-validation-for="@Model.person" class="text-danger"></span>*@
    <div class="form-group">
        <label>Select City</label>
       <select name="CityName" class="form-control" asp-items="ViewBag.Cities" ></select>
    </div>
    <div class="form-group">
        <label>Select Language</label>
       <select name="LanguageName" class="form-control" asp-items="ViewBag.Languages" ></select>
    </div>
    <input type="submit" value="Save changes" class="btn btn-primary" />
    </form>
}



